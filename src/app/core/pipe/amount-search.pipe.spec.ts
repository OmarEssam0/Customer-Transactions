import { AmountSearchPipe } from './amount-search.pipe';

describe('AmountSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AmountSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
